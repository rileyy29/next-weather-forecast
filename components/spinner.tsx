export default function Spinner() {
    return <div className={"animate-spin inline-block w-5 h-5 border-[2px] border-current border-t-transparent text-slate-500 rounded-full"} role={"status"} aria-label={"loading"} />;
}